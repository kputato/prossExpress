version: '3'
services:
  prossexpress:
    build:
      dockerfile: Dockerfile
      context: ..
    volumes:
      # Update this to wherever you want VS Code to mount the folder of your project
      - ..:/workspaces:cached
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]